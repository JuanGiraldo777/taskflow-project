# Dependencias de Node
node_modules/

# Credenciales y configuración sensible de Cursor/MCP
.cursor/mcp.json

# Variables de entorno (por si añades un .env en el futuro)
.env
.env.local
.env.production

# OJO: ejercicio_web/src/output.css SÍ se sube a propósito. Vercel no
# compila nada (buildCommand vacío en vercel.json): sirve ese archivo tal
# cual. Si se ignora, producción se queda sin estilos. Se regenera con
# `npm run build` desde ejercicio_web/src/style.css.

# Archivos de sistema operativo
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Configuración de VSCode
.vscode/
# Configuración de Claude
.claude
# Configuración de Playwright
.playwright